# Bothello-ai-agent

## Installation
In order to install all dependencies necessary to run the project, you should run the following command:

```Python
pip install -r "requirements.txt"
```

## Run the project
To run the code, you should execute the following command from the terminal from the root directory:

```Python
python main.py
```